import UserService from "../service/User"

function useUser(body){

    const getAllUser = ()=>{
        try{
            const response = await UserService.getAllPengguna();
            
        }
    }
}